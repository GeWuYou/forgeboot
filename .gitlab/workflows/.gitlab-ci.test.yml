stages:
  - publish
# 🧪 test 发布 SNAPSHOT 包（允许覆盖）
publish:
  stage: publish
  script:
    - echo "🔧 授予 gradlew 执行权限..."
    - chmod +x gradlew
    - ./gradlew clean publishMavenJavaPublicationToGitLabRepository --no-daemon --refresh-dependencies
  tags: [ java ]
